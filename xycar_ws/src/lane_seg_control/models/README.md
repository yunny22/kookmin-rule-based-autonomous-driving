# Local model placeholder

The final competition Xbin centerline weight is intentionally not included in
this public release. Supply an authorised local model through the
`model_path` parameter when reviewing the perception node. Camera calibration
and projective geometry must also be measured locally; the source defaults are
only non-vehicle example values.
